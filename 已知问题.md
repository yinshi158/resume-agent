# 已知问题

| 问题                  | 原定级  | 新定级       | 原因                                                                                |
| ------------------- | ---- | --------- | --------------------------------------------------------------------------------- |
| source_span 锚定层未定义  | 实现细节 | P0，M1 阻塞项 | M1 的核心交互就是"原文 span 高亮联动"。span 锚在 PDF 抽取文本层还是 Markdown 层、双栏乱序怎么处理不定死，M1 的主功能就是空中楼阁 |
| quote 归一化（全角/半角/空白） | 实现细节 | P1，M2 阻塞项 | diagnose 的"程序验证 quote 子串"在 M2 上线，不归一化会误报一片，诊断报告可信度直接崩                             |
| formula 安全求值        | 实现细节 | P2，M3 阻塞项 | 你整份文档都在防 eval；**已兑现**：白名单 AST（数字/四则/括号/百分号字面量），注入单测覆盖（调用/属性/名称/下标/比较/幂/布尔一律拒绝）          |
| 中文动词词表列为持续维护资产      | 实现细节 | P2，M3     | 归因动词检查依赖它，写进模块表；**已兑现**：`app/verb_lexicon/`（LEXICON_VERSION=1，主导级 21 / 参与级 20 词，命中 × 归因组合矩阵单测）  |
| gate2 确认行为落库        | 强烈建议 | 已兑现（M3）   | M3 上线 gate2 同批建 gate_events：confirm/reject/edit + 编辑前后文本 + **当次四层判定快照**，动作 × 判定配对可查（误报/漏报分析原料） |
| 双模板导出（网申版/直聘版）      | 建议   | 可以缓，但要写下来 | export 模块仍只写"ATS 友好模板"，v2 事项至少登记进文档，别丢                                            |

v0.3 新引入的三个小问题
--------------

1. **M1 的"不依赖 LLM"措辞要修**。解析入库本身靠 LLM 抽 schema，M1 真实的状态是"不依赖 LLM **调优**"，不是不依赖 LLM——mock 模式下规则抽取的中文简历解析质量会很差，演示时别拿 mock 模式的解析效果当真实水平。这是措辞问题，但写进里程碑文档就会误导优先级判断。
2. **M2/M3 需要进度态设计**。LLM 链路（诊断举证、改写-校验循环）是分钟级任务，本地应用也一样会卡在加载动画上。两个 gate 页面之外，诊断页和导出页要有流式进度或分步状态，这个在文档里完全没有着落。（M2 已兑现：诊断页 SSE 分步进度，见文末实现记录）
3. **人工标注测试集没有进里程碑**。文档"评估与防坑"里写了诊断与校验共用标注测试集，但 M1-M3 里没有它的位置。它是 M2 的前置依赖——没有 10-20 份标好的"JD-简历对 + 期望举证结果"，M2 的 prompt 效果无法验收，M3 的校验器阈值更是无从调起。建议在 M2 开工条件里写明。（已写入 spec §13"标注测试集"，框架与草稿已建，见文末实现记录）

v0.4 评审决议与建议排期
--------------

| 事项                                                                                    | 排期  | 状态                         |
| ------------------------------------------------------------------------------------- | --- | -------------------------- |
| 日期归一化移出 canonical_text：锚定层只做机械变换（NFKC/全半角/空白折叠），日期解释下沉抽取层，歧义走 anomaly 标记 + gate1 人工确认 | 已完成 | 已写入 v0.4 P0 节              |
| normalize 承重待遇：属性测试（幂等性 + 中文样本偏移稳定性快照），实现版本化，改动 = 全量重入库                               | M1  | 已兑现：hypothesis 属性测试 + 偏移快照测试（test_normalize.py） |
| gate1 阅读序逃生门："切换 MinerU 重新解析"按钮，避免"可见不可修"                                             | M1  | 已兑现：ReviewPage 重解析按钮（未安装置灰提示），重解析产生新 resume 行 |
| gate_events 带当次校验判定快照（误报/漏报分析依赖"判定 × 行为"配对）                                           | M3  | 已写入 M3 同步项与 gate_events 决策 |

M1 代码审查发现与修复（2026-09-16，63 后端 + 8 前端测试全绿）
--------------

| 问题 | 定级 | 状态 |
| --- | --- | --- |
| quote 多实例锚定错误：find_quote 静默取第一个匹配，重复文本（技能词等）高亮/证据指错位置 | P2 | 已修：新增 find_quote_matches 全匹配检测；入库时多处匹配不锚定（span -1）+ missing_field 歧义标记交人工；prompt 增加唯一性约束（短词带上下文）；M2 举证 quote 复用同一机制 |
| mock 溯源用全局状态：真实解析的简历在 mock 模式下查看会被误标 | P3 | 已修：mock 按行记录（parser='mock'），与当前全局模式无关 |
| percent_bound 负增长率规则可能误伤合理表述（如"将亏损收窄"） | P3 | 已修：措辞改"语义存疑，请人工判断"，与"宁可漏报不可误报"原则对齐 |
| MinerU 子进程在 Windows 按 GBK 解码输出，UTF-8 输出会抛 UnicodeDecodeError | P3 | 已修：subprocess 显式 encoding="utf-8" + errors="replace" |
| 测试运行时残留 UnicodeDecodeError 警告 | 环境 | 排查结论：非项目代码——上传链路零 subprocess（Popen 追踪验证），警告源自运行 pytest 的 hermes 运行时环境本身，不影响测试结果 |

M2 实现记录（2026-09-17，100 后端 [M1 63 + M2 37] + 17 前端 [M1 8 + M2 9] 测试全绿）
--------------

| 事项 | 状态 |
| --- | --- |
| diagnose 模块（api / core / jd / prompts / io / mock_rules + 既有 schemas / evidence / report） | 已完成：`POST /api/diagnoses` SSE 分步（step parsing → evidencing n/N → reporting → done/error）、`GET /api/diagnoses/{id}` 报告、历史列表、`POST /api/requirements/{id}/revive` 捞回（报告确定性重算，不重调 LLM） |
| SCHEMA_VERSION 2 → 3：diagnoses 追加 `mock` 列 | 已完成（诊断级 mock 溯源按行记录，呼应 M1 审查 P3；拆解 warnings 存 report JSON 顶层） |
| 前端 DiagnosePage / DiagnosisReportPage / 首页已校对简历入口 / quote 点击跳校对页定位高亮（`?span=`） | 已完成；浏览器真实走查通过（Vite 代理下 fetch+ReadableStream 解析 SSE 正常，捞回后 summary 即时更新） |
| 标注测试集（spec §13，M2 开工条件） | 框架就绪：10 对 mock 基线草稿（`tests/fixtures/annotated/`，`draft:true` 显式标记）+ 回归统计（每次 pytest 终端输出 direct 命中 / gap 误判）+ VCR 录制回放（`RECORD_VCR=1`，先录后放）；草稿吻合 35/42（≈83%），差异集中于 mock 词表召回局限，**待真实 LLM 录制后替换并回写 spec §13.2 数值门槛** |
| 草稿观测差异（记录备查） | ① mock 拆解粒度=分句（JD 一行一条时 1:1）；② "管理经验 / Redis / Kubernetes"等语义相近但无字面词命中的要求报 gap——方向保守（符合 ard/0004 不对称性：宁可错标缺口），M3 擦边压力不回归；③ 证据选择在同行多词时不保证最优（如 SQL 与 Python 同在技能表时引用含 Python 的行） |

M2 审查修复 · 第二轮（2026-09-17，用户审查 4 条：P2×2 + P3×2）
--------------

| 问题 | 定级 | 处理 |
| --- | --- | --- |
| gap 措辞过度断言："事实源中无相关经历"实际只验证了词法无重叠，会让用户放弃手动查看，切断 ard/0004 低成本确认点 | P2 | 已修：改为"未找到关键词重叠的经历，可从下方选择相关条目捞回"（evidence.py，措辞只声明词法事实 + 明确捞回入口） |
| 纯词法最近邻导致可达条目标成 gap（标注统计中 gap 误判集中于此） | P2 | 已修：新增降级候选 `fallback_candidates`（**读取时确定性计算、不落库**）：只取入库时唯一定位的条目（保证捞回可用）、经历类 section（work/project）优先 top-3；前端 gap 下拉分「可考虑的候选 / 全部条目」两组，措辞不声称"最接近"。注意：候选只改善捞回入口的可见性，**不改变 status 判定**——语义级 nearest 是 M3 的 L2 职责 |
| "有证据"标签承诺过头（程序只验逐字定位，验不了语义相关） | P3 | 已修：UI 改为"有直接引用"；报告页头部注明"引用仅经程序验证逐字定位，相关性将在改写校验阶段核验；「最接近 / 缺口」仅表示词法层面未命中，可人工捞回" |
| revive 与 pipeline 的证据门槛不一致（pipeline 要求 quote 唯一定位才算 direct，revive 允许多处匹配的 raw_quote） | P3 | 已决（方案 a）：保留现状 + 明确语义区分——revive = fact_id 锚定（人工判定，UI「人工确认」），pipeline direct = quote 唯一定位（程序判定，UI「有直接引用」）；注释（`core.revive_requirement`）/spec §12.6/UI 三处写明。理由：多处匹配条目在校对页无法消除歧义，严格门槛会堵死人工捞回（ard/0004） |

VCR 真实录制（2026-09-17，deepseek-flash，10 对 ≈50 次调用）
--------------

- 录制成功 10/10（`tests/fixtures/annotated/vcr/*.json`），回放跑通（11 项，1.7s）；
  录制路径从主库导入真实 LLM 设置（隔离测试库无 key），失败对也保存录制（finally）。
- 真实 LLM 草稿吻合 33/42（≈79%，mock 基线为 35/42）：**gap 误判 3 条**（mock 6 条），
  direct 命中 14/18。摘除"拆解召回 100%"（10 对要求文本全部匹配）。
- 暴露的能力边界（M2 已声明、M3 承接）：① 一条要求含多子项时，LLM 可用半对证据
  使整条 direct（007「Redis 与消息队列」← Flink+Kafka 行）；② 证据"逐字可验"≠
  "支持要求"（004 Java 经验 ← 不含 Java 的岗位行）——均为 L2 语义校验职责。
- 差异复核清单已交付用户（001/002/004/006/007 共 7 条差异，含 5 条建议修期望、
  2 条建议保留为 LLM 表现记录）；草稿 `draft=true` 在测试中只观测趋势。

M2 验收收口（2026-09-18，102 后端 + 18 前端测试全绿）
--------------

按 M2 审查提出的三步收口（录制 → 人工复核 → 门槛写回），本次完成全部三步：

| 步骤 | 结果 |
| --- | --- |
| 1. 真实录制 | 10/10 对 VCR 录制就绪（deepseek-flash），回归走回放，不被 API 可用性阻塞 |
| 2. 人工复核 9 条差异 | **6 条改期望值**：3 条草稿期望过高（001「团队管理经验」、002「算法团队管理」、007「Kubernetes」实为 gap）、1 条草稿低估（004「高并发系统稳定性」实为 direct，LLM 找到"交易链路稳定性建设"逐字证据）、1 条 LLM 更诚实（004「MySQL 优化」direct→nearest，技能行有 MySQL 无"优化"）、1 条 quote 断言放宽（004「Java 后端开发」的年限证据在岗位行、Java 在技能行，单 quote 无法同时锚两处） |
| 3. 门槛写回 spec §13.2 | 非边界条目吻合率 ≥ **0.85**（当前基线 **39/39 = 100%**，gap 误判 0，拆解召回 100%）；`draft` 标记全部移除 |

**3 条边界条目的处置（不等于"记下来就算了"）**：003「Spark」、006「SQL」、
007「Redis 与消息队列」标为 `boundary: m3-l2`——它们是 M3 的 L2 语义校验
验收输入，其中前两条是 LLM 保守漏引（简历有证据但未给 quote），第三条是
单条要求含多子项时的半对证据（Kafka 是消息队列、Redis 无证据）。处置方式
不是留作永久红项，而是：

- 边界条目不进吻合率分母，但**必须带 comment 说明为什么是机制边界**，
  测试断言边界总量 ≤5（防止 boundary 变成"藏不达标项的后门"）；
- 每次 pytest 单独输出边界的当前实际状态（`· 边界 ['Spark→nearest']`），
  M3 的 L2 落地后应逐条翻正；
- 替代了原方案（留在差异列表里）——长期存在的红项会训练大家忽略差异输出。

M3 实现记录（2026-09-18，191 后端 [M1+M2 102 + M3 89] + 27 前端 [M1+M2 18 + M3 9] 测试全绿）
--------------
| 事项 | 状态 |
| --- | --- |
| M3-1 确定性资产 | SCHEMA_VERSION 3 → 4（只追加 7 张新表：rewrites / rewrite_sentences / sentence_validations / gate_events / versions / applications / outcomes）；formula 白名单求值器（禁用 eval，注入全拒）；verb_lexicon v1（主导 21 / 参与 20 词，LEXICON_VERSION=1，命中 × 归因矩阵单测） |
| M3-2 改写校验链路 | `app/rewrite/`（SSE writing/validating round n/escalated → done；目标清单剔除纯 gap 进升级清单；≤2 轮重写分流：L0/verb/L1 可修复进重试、L2 不蕴含直接升级不重试；全部轮次完成才一次事务入库）；`app/validate/` 四层（L0 偷渡数字/实体、verb 归因、L1 衍生数字重算 + 字面量溯源 + 模糊量化词、L2 LLM 蕴含）；L1 纪律：**至少一个字面量可回溯到声明出处**（纯常数公式 = 偷渡的另一种写法，一律拒绝），运算常数放行 |
| L2 标注集（R5 / C9） | `tests/fixtures/l2/`：9 组（3 条 boundary seed + 6 组扩充），deepseek-flash 真实录制**首轮 9/9 全吻合**（`RECORD_VCR=1` 可重录）；3 条 boundary 逐条翻正（Spark/SQL 蕴含、Redis 半对证据不蕴含）并回写 spec §13.2/§16.5；无录制走 mock 只观测趋势（mock 基线 5/9，不代表真实水平） |
| M3-3 gate2 | `app/gate2/`：confirm/reject/edit → gate_events（动作 × 当次四层判定快照，快照取自最新一轮不重算）；编辑后确定性三层必跑，L2 按 R2 触发（数字 token 集合变化或相似度 < 0.8）否则沿用并入快照；导出咽喉 `assert_exportable`（有 pending → 422，无句子 → 422） |
| M3-4 出口 | `app/export/`（ATS 单模板 HTML → Playwright Chromium 出 PDF；渲染前 ats.self_check：超长行 / 表格字符 / 非标准章节名；文件落 data/exports/，versions 记行）；`app/ledger/` 三表 API（applications / outcomes 追加式，只记录不分析）；R4 兑现：playwright 进默认依赖（Chromium ~195MB+115MB 经 npmmirror 安装成功），设置接口返回 `playwright_available`，缺失时导出按钮置灰 + 安装指引 |
| 前端 | RewritePage（SSE 分步 + 历史）、Gate2Page（标红置前 / 层徽标 tooltip / 内联编辑 / 出处 `?span=` 跳校对页 / 升级清单 / 导出解锁 + 记录投递）、诊断报告页「开始改写」入口（worth_applying=false 保留但标风险）；路由 `#/rewrite/{diagnosisId}`、`#/gate2/{rewriteId}` |
| 真实 LLM 走查（验收） | pair 001 一次通过：真实诊断 4 目标 → 真实改写 13 句 → 四层复检（1 条 L2 正确拦截"出处没写推荐系统中"的加料）→ gate2 处置 → 导出 PDF 回读 13/13、标准章节名全命中 |
| 走查发现并修复（P2） | **L0 日期误报**：ingest 实体数字不收无单位小数（"2019.3"），句面日期成片误报"数字无出处"（首轮走查 4 句误伤）。修复：L0 数字抽取前屏蔽日期表达式（区间/年月日），方向=漏报；补日期类单测 + spec §16.2 补充口径 |
| 观察（重录参考） | pair 007 复跑真实 LLM 与 M2 录制时行为不同（本次全 gap vs 录制时 Redis direct）——真实 LLM 非确定性；M2 回归走 VCR 回放不受影响，重录时须按 §13.2 复核差异 |
